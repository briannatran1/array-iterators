/*Write a function, doubleValues, which accepts an array of numbers, nums, and returns a new array containing those numbers doubled.*/

//use map to create a new arr with each val doubled

function doubleValues(nums) {
    return nums.map(val => val * 2);
}
